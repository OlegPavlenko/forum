<%= form_for @hat do |f| %>

  <% if @hat.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@hat.errors.count, "error") %> prohibited
        this hat from being saved:
      </h2>
      <ul>
        <% @hat.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.text_field :title, placeholder: "thread", maxlength: 6 %>
  </p>

  <p>
    <%= f.text_area :content, placeholder: "hat" %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
